[project]
name = "maintainers-copilot-backend"
version = "0.0.0"
description = "Maintainer's Copilot backend"
requires-python = ">=3.12,<3.13"
dependencies = [
    "alembic",
    "asyncpg",
    "httpx",
    "hvac",
    "langfuse",
    "python-dotenv",
    "pyyaml",
    "pydantic-settings",
    "sqlalchemy",
    "fastapi>=0.136.1",
    "uvicorn[standard]>=0.47.0",
    "fastapi-users[sqlalchemy]>=15.0.5",
    "redis>=7.4.0",
    "anthropic>=0.104.0",
    "sse-starlette>=3.4.4",
    "python-multipart>=0.0.29",
]

[dependency-groups]
dev = [
    "mypy",
    "pytest",
    "pytest-asyncio",
    "pytest-cov",
    "ruff",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "ASYNC", "S"]
ignore = ["S101"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "-q"

[tool.mypy]
python_version = "3.12"
strict = true
warn_unused_configs = true
ignore_missing_imports = true
